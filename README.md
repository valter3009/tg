# GidMeteo - Telegram Weather Bot v2.0

Профессиональный Telegram бот для получения информации о погоде и персонализированных советов по одежде с поддержкой часовых поясов.

## Новое в версии 2.0

- ⚡ **Полностью переписанная архитектура** - модульная структура с разделением на сервисы
- 🗄️ **SQLite/PostgreSQL** вместо JSON/Excel - надежное хранение данных
- 🌍 **Автоматическое определение часовых поясов** - уведомления приходят по местному времени пользователя
- 🔒 **Безопасность** - все токены в переменных окружения, никаких секретов в коде
- 🔄 **Retry логика** - автоматические повторы при ошибках API
- 📊 **Расширенная аналитика** - полная статистика в базе данных
- ♻️ **Миграция данных** - все пользователи автоматически перенесены из старой версии

## Возможности

- 🌤️ Актуальная информация о погоде по городам
- 👕 Персонализированные советы по одежде с учетом погоды, времени года и времени суток
- 💨 Информация о скорости ветра с юмористическими описаниями
- 📊 Отслеживание активности пользователей в БД
- 🔄 Автоматические обновления каждые 4 часа **по местному времени пользователя**
- 🌐 Inline режим - отправка прогнозов в любой чат
- 📍 Сохранение избранных городов
- 🕐 Интеллектуальное определение часового пояса по городу

## Архитектура проекта

```
tg/
├── bot/
│   ├── __init__.py
│   ├── config.py              # Конфигурация (без токенов!)
│   ├── database.py            # Подключение к БД
│   ├── models.py              # SQLAlchemy модели
│   ├── handlers/              # Обработчики команд
│   │   └── __init__.py
│   ├── services/              # Бизнес-логика
│   │   ├── __init__.py
│   │   ├── weather.py         # Сервис погоды с кэшированием
│   │   ├── notifications.py   # Уведомления с timezone
│   │   ├── analytics.py       # Аналитика и статистика
│   │   └── timezone.py        # Работа с часовыми поясами
│   └── utils/                 # Утилиты
│       ├── __init__.py
│       ├── clothes_advice.py  # Советы по одежде
│       ├── helpers.py         # Вспомогательные функции
│       └── retry.py           # Retry логика
├── main.py                    # Точка входа
├── migrate_data.py            # Скрипт миграции данных
├── requirements.txt
├── .env.example
├── .gitignore
├── Procfile                   # Для Railway/Heroku
└── README.md
```

## Установка

### 1. Клонируйте репозиторий

```bash
git clone https://github.com/valter3009/tg.git
cd tg
```

### 2. Создайте виртуальное окружение (рекомендуется)

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Настройте переменные окружения

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите ваши токены:

```env
TELEGRAM_TOKEN=ваш_токен_от_BotFather
OPENWEATHER_API_KEY=ваш_ключ_от_openweathermap
DATABASE_URL=sqlite:///gidmeteo.db
```

**Где получить:**
- `TELEGRAM_TOKEN`: [@BotFather](https://t.me/BotFather) → /newbot
- `OPENWEATHER_API_KEY`: [openweathermap.org/api](https://openweathermap.org/api) → Sign Up → API Keys

**⚠️ ВАЖНО:** Файл `.env` НЕ добавляется в Git (уже в `.gitignore`)

### 5. Инициализация базы данных

База данных создается автоматически при первом запуске. Если у вас есть данные из старой версии:

```bash
python migrate_data.py
```

Скрипт автоматически перенесет:
- Всех пользователей из `all_users.json`
- Города и избранное из `user_cities.json`
- Кэш погоды из `weather_cache.json`
- Определит часовые пояса по городам пользователей

## Запуск бота

```bash
python main.py
```

При успешном запуске вы увидите:

```
Запуск GidMeteo бота v2.0
Инициализация базы данных...
База данных успешно инициализирована
Запуск Flask сервера на порту 8000...
Запуск задачи автоматического обновления...
Бот GidMeteo запущен. Нажмите Ctrl+C для остановки.
Автоматические обновления с учетом часовых поясов пользователей
```

## 🚀 Деплой на Railway.app

Railway.app - простой способ запустить бота 24/7 в облаке. **Бесплатно:** $5 кредитов каждый месяц.

### Пошаговая инструкция:

#### 1. Подготовка (✅ уже сделано)

Все файлы для деплоя в репозитории:
- `Procfile` - команда запуска
- `requirements.txt` - зависимости
- `.env.example` - пример переменных окружения

#### 2. Регистрация на Railway

1. Откройте [railway.app](https://railway.app)
2. Нажмите **"Start a New Project"**
3. Войдите через GitHub
4. Разрешите Railway доступ к репозиториям

#### 3. Создание проекта

1. Нажмите **"New Project"**
2. Выберите **"Deploy from GitHub repo"**
3. Найдите репозиторий **`valter3009/tg`**
4. Railway автоматически определит Python проект

#### 4. Настройка переменных окружения

В проекте Railway откройте **"Variables"** и добавьте:

```
TELEGRAM_TOKEN=ваш_токен_бота
OPENWEATHER_API_KEY=ваш_ключ_openweathermap
DATABASE_URL=sqlite:///gidmeteo.db
```

**Опционально (для PostgreSQL на Railway):**

1. Добавьте PostgreSQL плагин в проект
2. Railway создаст переменную `DATABASE_URL` автоматически

#### 5. Деплой (автоматически)

Railway автоматически:
- ✅ Установит зависимости
- ✅ Запустит миграцию данных (если есть JSON файлы)
- ✅ Запустит бота
- ✅ Перезапустит при падении

#### 6. Проверка

1. Откройте **"Deployments"** → View Logs
2. Найдите: `"Бот GidMeteo запущен"`
3. Напишите боту `/start` в Telegram

### 🎉 Готово! Бот работает 24/7

## Особенности работы с часовыми поясами

### Как это работает

1. **Автоматическое определение:** При добавлении первого города бот определяет часовой пояс пользователя
2. **Локальное время:** Уведомления приходят в 00:01, 04:01, 08:01, 12:01, 16:01, 20:01 **по местному времени**
3. **Глобальное покрытие:** Поддержка всех часовых поясов мира через базу IANA

### Примеры

- Пользователь в Москве (UTC+3): получит уведомление в 00:01 по московскому времени
- Пользователь в Нью-Йорке (UTC-5): получит уведомление в 00:01 по нью-йоркскому времени
- Пользователь без города: уведомление по UTC

## Команды бота

### Для пользователей:
- `/start` - Запустить бота и показать главное меню
- Отправка названия города - Получить погоду и советы по одежде

### Для администраторов:
- `/stats` - Подробная статистика (пользователи, активность)

### Inline режим:
В любом чате наберите `@MeteoblueBot название_города` для отправки прогноза погоды

## База данных

### Модели

- **User** - пользователи (telegram_id, timezone, is_active)
- **City** - города
- **UserCity** - связь пользователь-город (избранное)
- **WeatherCache** - кэш погодных данных
- **UserActivity** - логи активности
- **AutoUpdateLog** - статистика автообновлений

### Миграция на PostgreSQL

Для production рекомендуется PostgreSQL:

```env
DATABASE_URL=postgresql://user:password@localhost:5432/gidmeteo
```

Миграция данных:

```bash
# 1. Создайте PostgreSQL БД
createdb gidmeteo

# 2. Обновите DATABASE_URL в .env
# 3. Запустите бота - таблицы создадутся автоматически
python main.py
```

## Технологии

- **Python 3.11+**
- **pyTelegramBotAPI 4.14.0** - Telegram Bot API
- **SQLAlchemy 2.0.23** - ORM для работы с БД
- **requests 2.31.0** - HTTP запросы
- **pytz 2023.3** - Работа с часовыми поясами
- **geopy 2.4.1** - Геокодирование
- **timezonefinder 6.2.0** - Определение часового пояса по координатам
- **python-dotenv 1.0.0** - Управление переменными окружения
- **Flask 3.0.0** - Keepalive для Railway
- **OpenWeatherMap API** - Данные о погоде

## Безопасность

### ✅ Что сделано

- Все токены и API ключи в переменных окружения
- `.env` файл в `.gitignore`
- Никаких секретов в коде
- Безопасное хранение данных в БД
- Валидация входных данных

### ⚠️ Важно

- Никогда не коммитьте файл `.env`
- Регулярно обновляйте зависимости
- Используйте HTTPS для БД в production
- Ограничьте доступ к админ-командам

## Возможные проблемы

### Бот не запускается

- Проверьте `.env` файл (скопируйте из `.env.example`)
- Убедитесь что токены корректны
- Установите зависимости: `pip install -r requirements.txt`

### Не работает погода

- Проверьте API ключ OpenWeatherMap
- Убедитесь что ключ активен на openweathermap.org
- Проверьте интернет-соединение

### Ошибки timezone

- Geopy использует бесплатный Nominatim с лимитами
- При превышении лимитов timezone останется UTC
- Можно настроить свой Nominatim сервер

### Миграция данных

- JSON файлы должны быть в корне проекта
- Формат файлов должен соответствовать старой версии
- При ошибках проверьте логи: `python migrate_data.py`

## Мониторинг

### Логи

```bash
# Основные логи
tail -f bot_errors.log

# Логи Railway
railway logs
```

### Статистика

Используйте команду `/stats` для получения:
- Общей статистики пользователей
- Активности за последние 7 дней
- Топ городов и пользователей

## Лицензия

Проект создан для личного использования.

## Автор

Telegram: [@GidMeteoBot](https://t.me/MeteoblueBot)

---

## Changelog v2.0

### Added
- ✨ Автоматическое определение часовых поясов по городам
- ✨ Уведомления по местному времени пользователя
- ✨ SQLAlchemy ORM для работы с БД
- ✨ Модульная архитектура (services, handlers, utils)
- ✨ Retry логика для API запросов
- ✨ Расширенная аналитика в БД
- ✨ Скрипт автоматической миграции данных

### Changed
- 🔄 Переход с JSON/Excel на SQLite/PostgreSQL
- 🔄 Полный рефакторинг кодовой базы
- 🔄 Улучшенное кэширование погоды
- 🔄 Оптимизированные запросы к БД

### Security
- 🔒 Все токены в переменных окружения
- 🔒 Удалены секреты из кода
- 🔒 Обновлен .gitignore для защиты данных

### Removed
- ❌ Удалены дублирующиеся файлы
- ❌ Удален хардкод списка пользователей
- ❌ Удалены Excel файлы логов
