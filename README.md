# GidMeteo - Telegram Weather Bot

Telegram бот для получения информации о погоде с персонализированными советами по одежде.

## Возможности

- ☀️ Получение текущей погоды для любого города
- 👔 Интеллектуальные советы по одежде с учетом:
  - Температуры (от -40°C до +40°C)
  - Погодных условий (дождь, снег, туман и т.д.)
  - Сезона и времени суток
  - Скорости ветра с юмористическими описаниями
- 🌍 Поиск по городам с сохранением избранных
- 📱 Веб-приложение интегрированное в Telegram
- 💾 Кэширование данных для оптимизации запросов
- 📊 Статистика использования и аналитика

## Технологический стек

**Backend:**
- Python 3.9+
- pyTelegramBotAPI (telebot)
- OpenWeatherMap API
- pandas, openpyxl для аналитики

**Frontend:**
- HTML5, CSS3, JavaScript
- Telegram WebApp API
- Yandex.Metrika

## Развертывание на Railway

### Шаг 1: Подготовка

1. Создайте Telegram бота через [@BotFather](https://t.me/BotFather)
2. Получите API ключ на [OpenWeatherMap](https://openweathermap.org/api)
3. Форкните этот репозиторий или клонируйте его

### Шаг 2: Настройка Railway

1. Зарегистрируйтесь на [Railway](https://railway.app)
2. Создайте новый проект
3. Подключите свой GitHub репозиторий

### Шаг 3: Настройка переменных окружения

В настройках проекта Railway добавьте следующие переменные окружения:

```
TELEGRAM_TOKEN=ваш_токен_telegram_бота
OPENWEATHER_API_KEY=ваш_api_ключ_openweathermap
```

### Шаг 4: Деплой

Railway автоматически деплоит приложение после коммита в основную ветку.

## Локальная разработка

### Установка

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/tg.git
cd tg

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл из примера
cp .env.example .env
```

### Настройка .env

Отредактируйте `.env` файл и добавьте ваши токены:

```
TELEGRAM_TOKEN=ваш_токен
OPENWEATHER_API_KEY=ваш_ключ
```

### Запуск

```bash
python main.py
```

## Структура проекта

```
tg/
├── main.py                 # Основной файл бота
├── clothes_advice.py       # Модуль с советами по одежде
├── activity_tracker.py     # Трекер активности пользователей
├── index.html             # Основное веб-приложение
├── meteo.html             # Упрощенная версия
├── requirements.txt       # Зависимости Python
├── Procfile              # Конфигурация для Railway
├── railway.json          # Настройки Railway
├── .env.example          # Пример переменных окружения
├── .gitignore            # Игнорируемые файлы
└── README.md             # Документация
```

## Команды бота

- `/start` - Начать работу с ботом
- `/stats` - Статистика использования (только для администраторов)
- `/check_users` - Проверка пользователей (только для администраторов)

## Важные заметки

### Хранение данных

Бот создает следующие файлы для хранения данных:
- `user_cities.json` - Сохраненные города пользователей
- `weather_cache.json` - Кэш погодных данных
- `all_users.json` - База всех пользователей
- `weather_bot.db` - SQLite база данных
- `*.log` - Логи работы бота
- `*.xlsx` - Отчеты активности

### Миграция данных

При переносе бота сохраните следующие файлы:
- `user_cities.json`
- `all_users.json`
- `weather_bot.db`

Railway предоставляет эфемерное хранилище, поэтому для персистентных данных рекомендуется использовать:
- Railway Volume для файлового хранилища
- Или внешнюю базу данных (PostgreSQL, MongoDB)

## Лицензия

MIT License

## Автор

GidMeteo Weather Bot

## Поддержка

Если у вас возникли проблемы, создайте Issue в репозитории.

---

Сделано с ❤️ для любителей погоды
